apiVersion: apps/v1
kind: Deployment
metadata:
  name: webhook-regex-app-3
  namespace: default
spec:
  replicas: 4
  selector:
    matchLabels:
      app: webhook-regex-app-3
  template:
    metadata:
      labels:
        app: webhook-regex-app-3
    spec:
      containers:
      - name: nginx
        image: nginx:alpine
        ports:
        - containerPort: 80
