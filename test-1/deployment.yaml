apiVersion: apps/v1
kind: Deployment
metadata:
  name: webhook-regex-app-1
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: webhook-regex-app-1
  template:
    metadata:
      labels:
        app: webhook-regex-app-1
    spec:
      containers:
      - name: nginx
        image: nginx:alpine
        ports:
        - containerPort: 80
