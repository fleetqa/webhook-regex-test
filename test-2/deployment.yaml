apiVersion: apps/v1
kind: Deployment
metadata:
  name: webhook-regex-app-2
  namespace: default
spec:
  replicas: 3
  selector:
    matchLabels:
      app: webhook-regex-app-2
  template:
    metadata:
      labels:
        app: webhook-regex-app-2
    spec:
      containers:
      - name: nginx
        image: nginx:alpine
        ports:
        - containerPort: 80
